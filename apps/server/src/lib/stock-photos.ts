// Removed. Pexels stock-photo search is no longer used anywhere in the app —
// all slide backgrounds now come from the R2 image library (see library.ts),
// wired in via generate-slideshow.ts. This file is intentionally left empty so
// any stray import fails loudly instead of silently reintroducing an external
// image API. Safe to delete outright.
export {};
